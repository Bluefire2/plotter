import React from 'react';

const VariableInput = ({placeholder}) => {
    return (
        <textarea id="variable-input" placeholder={placeholder}>
        </textarea>
    );
};

export default VariableInput;